// import { ABOUT_URL } from "@/config/app-config";
// import { ResponseData } from "./types/content/collection";

// export async function getAbout(query: string = ""): Promise<ResponseData> {
//   const res = await fetch(`${ABOUT_URL}?${query}`);

//   if (!res.ok) {
//     // This will activate the closest `error.js` Error Boundary
//     throw new Error("Failed to fetch users");
//   }

//   return res.json();
// }
